/**
 * Agentplace SDK for agent-dev-server
 *
 * Provides platform integration utilities for agents running in the Agentplace runtime.
 */

export {
  getSessionIdFromRequest,
  generateSessionId,
  extractSessionIdentity,
  type SessionIdentity,
} from './session-id';
